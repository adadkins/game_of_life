This directory uses SSE events to push the board to the front end in the SSE payload

This has to use a little different strategy since the payload is multiple lines, 
as well as passing the object down to the go html fragment.